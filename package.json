{
  "name": "@fluss/web",
  "version": "0.2.0",
  "description": "Functions for functional programming in browser.",
  "keywords": [
    "functional web",
    "typescript"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/YevhenKap/fluss-web"
  },
  "main": "index.js",
  "module": "index.mjs",
  "scripts": {
    "clean": "rm -r dist/*",
    "build": "npm run test; npm run clean; rollup -c; npm run copy",
    "copy": "npm run copy:settings; npm run copy:docs; npm run copy:declaration; npm run copy:license",
    "copy:settings": "cp package.json dist/",
    "copy:license": "cp LICENSE dist/",
    "copy:docs": "cp README.md dist/; cp CHANGELOG.md dist/",
    "copy:declaration": "cp -r src/declaration/ dist/",
    "release": "npm run build; npm publish dist/ --access public",
    "test": "jest"
  },
  "author": "Kapelianovych Yevhen",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-typescript": "^5.0.2",
    "@types/jest": "^26.0.9",
    "jest": "^26.4.0",
    "rollup": "^2.23.1",
    "rollup-plugin-terser": "^7.0.0",
    "ts-jest": "^26.2.0",
    "tslib": "^2.0.1",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "@fluss/core": "^0.4.0"
  }
}
