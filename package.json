{
  "name": "@fluss/web",
  "version": "0.7.0",
  "description": "Functions for functional programming in browser.",
  "keywords": [
    "functional web",
    "typescript"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/YevhenKap/fluss-web"
  },
  "main": "index.js",
  "scripts": {
    "clean": "mkdir -p build; rm -r build/*",
    "build": "npm run clean; rollup -c; npm run copy; npm run test",
    "copy": "npm run copy:settings; npm run copy:docs; npm run copy:declaration; npm run copy:license",
    "copy:settings": "cp package.json build/",
    "copy:license": "cp LICENSE build/",
    "copy:docs": "cp README.md build/; cp CHANGELOG.md build/",
    "copy:declaration": "cp -r src/declaration/ build/",
    "release": "npm run build; npm publish build/ --access public",
    "test": "jest"
  },
  "author": "Kapelianovych Yevhen",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.12.3",
    "@babel/preset-env": "^7.12.1",
    "@rollup/plugin-typescript": "^6.0.0",
    "@types/jest": "^26.0.15",
    "babel-jest": "^26.6.0",
    "jest": "^26.6.0",
    "rollup": "^2.32.1",
    "rollup-plugin-terser": "^7.0.2",
    "ts-jest": "^26.4.1",
    "tslib": "^2.0.3",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@fluss/core": "^0.8.0"
  }
}
